1. Dockerfile загрузил в репозиторий
    sudo docker build . -t django
    sudo docker run --name django --rm -p 8000:8000 django
    
2. С миграциями не разобрался. Посмотрел разбор полетов на уроке, пока не въехал. Тупо повторять не хочу, нужно время, чтобы понять.
3. В файле /etc/hosts добавил строчку 
   127.0.0.1    django
   В браузере ружно набирать django:8000
4. Пока не разобрался.
   